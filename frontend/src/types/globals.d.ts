export {};

declare global {

    interface IUser {
        id: number
        name: string
        surname: string
    }

    // type NextColor = "default" | "primary" | "secondary" | "success" | "warning" | "danger";
}